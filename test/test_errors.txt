import std.io;

class ErrorTest {
    int value;

    void process(int x) {
        // خطای معنایی ۱: تعریف مجدد متغیر در یک Scope
        int x; 
        
        // خطای معنایی ۲: استفاده از متغیر تعریف‌نشده
        z = 20; 
        
        // خطای معنایی ۳: عدم تطابق نوع در انتساب (رشته به int)
        value = "string_value"; 
        
        int temp;
        temp = 5;
    }

    void run() {
        // خطای معنایی ۴: فراخوانی متد با تعداد پارامترهای نادرست
        process(); 
        
        // خطای معنایی ۵: فراخوانی متد با نوع پارامتر اشتباه (فرستادن رشته به جای int)
        process("hello"); 
    }
}
