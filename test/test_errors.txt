import UndefinedClass;

class Main {
    public static void main(String[] args) {

        ErrorTest obj;
        obj = new ErrorTest();
        int r;

        r = obj.compute();

        r = obj.compute("hello", 'c');

        r = obj.compute('a', 5);

        z = 20;

        if (r > 0) {
            int inner;
            inner = 1;
        }
        inner = 2;

        int[] arr;
        arr = new int[5];
        arr[-3] = 1;
        arr[10] = 2;
        arr[true] = 3;
    }
}

class ErrorTest {

    private int value;
    private int value;

    public int compute(int a, char c) {
        int a;

        int temp;
        temp = 5;
        int temp;

        value = "wrong_type";

        return "invalid_return";
    }

    public boolean check(int k) {
        return k + 1;
    }
}

class A extends B {
}

class B extends A {
}
